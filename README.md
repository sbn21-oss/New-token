# Quick setup — if you've done this kind of thing before
# Copy these commands:

echo "# flash-token" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/sbn21-oss/New-token.git
git push -u origin main# New-token
